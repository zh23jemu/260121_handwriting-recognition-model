version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: handwriting_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: handwriting_db
      MYSQL_USER: handwriting_user
      MYSQL_PASSWORD: handwriting_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: unless-stopped

volumes:
  mysql_data:
